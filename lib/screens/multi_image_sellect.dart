import 'package:flutter/material.dart';

class MultiImageSellect extends StatelessWidget {
  const MultiImageSellect({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Expanded(child: Container(color: Colors.red)));
  }
}
