import 'package:flutter/material.dart';

class MultiSellection extends StatelessWidget {
  const MultiSellection({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Expanded(child: Container(color: Colors.amber)));
  }
}
