import 'package:flutter/material.dart';

class ToggleSellection extends StatelessWidget {
  const ToggleSellection({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Expanded(child: Container(color: Colors.green)));
  }
}
